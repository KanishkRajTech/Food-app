{
  "name": "foodapp",
  "version": "1.0.0",
  "private": true,
  
  "scripts": {
  "dev": "npm-run-all --parallel server client",
    "server": "npm --prefix ./backend run dev",
    "client": "npm --prefix ./frontend/food-app start"
},
"devDependencies": {
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  }
}

